"use strict";
/*
'use strict'
import {encode} from '../src/index'

for (let q = 0; q < 65535; q++) {
    const c = String.fromCharCode(q)
    let e1, e2
    try { e1 = encodeURIComponent(c) } catch (ex) { e1 = '??' }
    try { e2 = encode(c) } catch (ex) { e2 = '??' }
    if (e1 === e2) continue
    console.log(`${e1} <> ${e2}`)
}
*/
//# sourceMappingURL=test1.js.map